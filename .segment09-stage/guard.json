{"baseTree":"848b14e337552c39fbc847c7d9dc366f77a8096c","expectedTree":"27761a0f652c441b8a6ad05a369cbff031d664d1","files":[{"path":"test-bank-analytics-dashboard.js","before":"184aade830870c732596d5e1a83a33f2961f97e8","after":"699498c588c020214a94c6679cc8d5ed8975d826","edits":[[11104,11104,"  // Malformed cache entries are not graded sessions. Keep valid evidence\n  // usable without rewriting the cache or fabricating replacement results.\n  function attemptEntries() {\n    const entries=examData(readStore()).attempts;\n    return Array.isArray(entries)?entries.filter(function(entry){return entry && typeof entry==='object' && !Array.isArray(entry);}):[];\n  }\n\n"],[11246,11317,"    return attemptEntries().slice().sort(function (left, right) {\n"],[12821,12893,"    return attemptEntries().map(function(entry) {\n"],[13381,13583,"    return attemptEntries().filter(function(entry){return entry && !entry.versionPin && entry.mode==='exam' && entry.timed===true && entry.completed===true;}).map(function(entry){\n"],[16882,17013,"    const attempt=attemptEntries().find(function(entry){return entry && String(entry.id)===String(last.id);});\n"],[17328,17482,"    let compatible=false;\n    try {compatible=!!(payload && payload.versionPin && window.__TBVersions.canonical(payload.versionPin)===window.__TBVersions.canonical(last.versionPin));}\n    catch(error) { /* Invalid retained payload: use validated original aggregates. */ }\n"]]},{"path":"tests/test-bank-segment09-grading.test.js","before":"2679e87c6616c75db0c43b97cbdb2eaf562d4071","after":"60aaf1ec2ba338189d020b801176fa7cfd44dc7d","edits":[[16623,16623,"\ntest('09 malformed retained history cannot crash valid score projections or replace original groups',()=>{\n  const f=fixture(),a=attempt(f),s=analytics();s.setExamData({attempts:[null,7,[],a]});\n  assert.equal(s.context.__TBAnalyticsDashboard.examAttemptSeries().length,1);\n  assert.equal(s.context.__TBAnalyticsDashboard.sessionTrend().length,1);\n  const unsafePin=JSON.parse('{\"constructor\":\"untrusted\"}');\n  s.context.__TBLearning={eventsForExam:()=>[{id:'bad-pin',type:'session_completed',sessionId:a.id,occurredAt:STAMP,payload:{versionPin:unsafePin}}]};\n  const rows=plain(s.context.__TBAnalyticsDashboard.latestExamDomainBreakdown());\n  assert.equal(rows.reduce((n,d)=>n+d.total,0),3);assert.equal(rows.reduce((n,d)=>n+d.correct,0),1);\n  assert.equal(V.assessAttempt(null).available,false);\n});\n"]]}]}