<!DOCTYPE html>
<html lang="en">
<!-- UPSKILLSPRINT_LESSON_META
{
  "title": "The Normal Distribution: Meet the Bell Curve",
  "slug": "the-normal-distribution-meet-the-bell-curve",
  "category": "Statistics",
  "category_slug": "statistics",
  "level": "Beginner",
  "estimated_minutes": 15,
  "interactive": true,
  "card_title": "The Normal Distribution",
  "card_description": "Meet the bell curve: master the 68-95-99.7 rule and z-scores with live, hands-on tools.",
  "search_keywords": [
    "normal distribution",
    "bell curve",
    "gaussian",
    "empirical rule",
    "68 95 99.7",
    "z-score",
    "standard deviation",
    "probability",
    "statistics basics"
  ],
  "suggested_github_path": "lessons/statistics/the-normal-distribution-meet-the-bell-curve.html"
}
-->
<head>
<!-- EDIT: Lesson metadata -->
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>The Normal Distribution: Meet the Bell Curve | UpSkill Sprint Consulting</title>
<meta name="description" content="A beginner-friendly, interactive lesson on the normal distribution: the bell curve, the 68-95-99.7 rule, z-scores, and how to use them at work.">
<link rel="canonical" href="https://upskillsprint.com/lessons/statistics/the-normal-distribution-meet-the-bell-curve">
<meta property="og:type" content="article">
<meta property="og:title" content="The Normal Distribution: Meet the Bell Curve | UpSkill Sprint Consulting">
<meta property="og:description" content="A beginner-friendly, interactive lesson on the normal distribution, the 68-95-99.7 rule, and z-scores.">
<meta property="og:image" content="https://upskillsprint.com/assets/logo-icon.png">
<meta property="og:url" content="https://upskillsprint.com/lessons/statistics/the-normal-distribution-meet-the-bell-curve">
<meta name="twitter:card" content="summary">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:opsz,wght@8..60,500;8..60,600;8..60,700&family=Work+Sans:wght@400;500;600&display=swap" rel="stylesheet">
<link rel="stylesheet" href="/style.css">
<link rel="stylesheet" href="/lessons-theme.css">

<!-- EDIT: Lesson styles (single style block) -->
<style>
/* ============ Design tokens ============ */
:root {
  --paper: #ffffff;
  --surface: #ffffff;
  --surface-soft: #f5f7fa;
  --card: #ffffff;
  --tint: #f5f7fa;
  --ink: #101828;
  --ink-soft: #344054;
  --muted: #667085;
  --line: #e3e7ee;
  --navy: #0f2a43;
  --teal: #0e7490;
  --teal-dark: #0a5a70;
  --amber: #d97706;
  --success: #2e8b57;
  --warning: #b97a17;
  --danger: #bf3b2b;
  --info: #2f6fb0;
  --shade-1: rgba(15, 139, 141, 0.42);
  --shade-2: rgba(15, 139, 141, 0.22);
  --shade-3: rgba(15, 139, 141, 0.10);
  --focus-ring: 3px solid var(--amber);
}
html[data-theme="dark"] {
  --paper: #0b1220;
  --surface: #111c2d;
  --surface-soft: #0f1a2b;
  --card: #111c2d;
  --tint: #0f1a2b;
  --ink: #f4f7fb;
  --ink-soft: #d2dbea;
  --muted: #a9b6c8;
  --line: #2b3b50;
  --navy: #102a43;
  --teal: #2bb7c9;
  --teal-dark: #67d4df;
  --amber: #f0c36a;
  --success: #58c48a;
  --warning: #e9a94a;
  --danger: #e57368;
  --info: #6ea3d8;
  --shade-1: rgba(62, 196, 198, 0.45);
  --shade-2: rgba(62, 196, 198, 0.24);
  --shade-3: rgba(62, 196, 198, 0.11);
}

/* ============ Base ============ */
* { box-sizing: border-box; }
html { scroll-behavior: smooth; }
body {
  margin: 0;
  background: var(--paper);
  color: var(--ink);
  font-family: 'Work Sans', Arial, sans-serif;
  line-height: 1.65;
  font-size: 1.02rem;
}
h1, h2, h3, h4 { color: var(--ink); line-height: 1.25; font-family: 'Source Serif 4', Georgia, serif; font-weight: 600; }
h1 { font-size: clamp(1.7rem, 4vw, 2.4rem); margin: 0.4em 0; }
h2 { font-size: clamp(1.45rem, 3vw, 1.9rem); margin-top: 0; border-bottom: 3px solid var(--amber); display: inline-block; padding-bottom: 0.22em; }
h3 { font-size: 1.15rem; }
p { margin: 0.7em 0; }
a { color: var(--teal-dark); }
code {
  background: var(--surface-soft);
  border: 1px solid var(--line);
  border-radius: 4px;
  padding: 0.08em 0.35em;
  font-size: 0.92em;
  color: var(--ink);
}
strong { color: var(--ink); }

/* Visible focus for keyboard users */
a:focus-visible, button:focus-visible, input:focus-visible,
select:focus-visible, [role="switch"]:focus-visible {
  outline: var(--focus-ring);
  outline-offset: 2px;
}

/* Skip link */
.skip-link {
  position: absolute;
  left: -9999px;
  top: 0;
  background: var(--navy);
  color: #fff;
  padding: 0.6em 1em;
  z-index: 100;
  border-radius: 0 0 8px 0;
}
.skip-link:focus {
  left: 0;
}

/* ============ SITE INTEGRATION: Header ============ */
header.site.lesson-sitebar {
  z-index: 50;
}
.lesson-sitebar .header-actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 10px;
}
.lesson-sitebar .theme-control {
  flex-shrink: 0;
}

/* ============ Layout ============ */
main#lesson-content { max-width: 1120px; margin: 0 auto; padding: 2.25rem clamp(1rem, 4vw, 3.5rem) 4.5rem; }
article.lesson-wrapper { display: block; }
section { margin: 3rem 0; scroll-margin-top: 92px; }

/* Lesson hero */
.lesson-hero {
  margin-top: 0;
  padding: clamp(2rem, 5vw, 3.25rem);
  border-radius: 14px;
  background: linear-gradient(135deg, #0f2a43, #0e7490);
  box-shadow: 0 10px 30px rgba(15, 42, 67, 0.16);
  color: #ffffff;
}
.lesson-hero h1 { color: #ffffff; }
.lesson-hero .lesson-lede { color: #e0f2fe; }
.lesson-hero .why-it-matters {
  background: rgba(255, 255, 255, 0.12);
  border-left-color: #f0c36a;
  color: #f8fafc;
}
.lesson-hero .why-it-matters strong { color: #ffffff; }
.lesson-meta-row { display: flex; flex-wrap: wrap; gap: 0.5rem; margin: 0.6rem 0; }
.pill {
  background: rgba(255,255,255,.12);
  border: 1px solid rgba(255,255,255,.28);
  color: #f8fafc;
  border-radius: 999px;
  padding: 0.25rem 0.8rem;
  font-size: 0.82rem;
  font-weight: 700;
}
.pill.accent { background: #ffffff; color: #0f2a43; border-color: rgba(255,255,255,.7); }
html[data-theme="dark"] .pill.accent { color: #0f2a43; }
.lesson-lede { font-size: 1.12rem; color: var(--ink-soft); max-width: 62ch; }
.why-it-matters {
  border-left: 4px solid var(--amber);
  background: var(--surface);
  padding: 0.8rem 1rem;
  border-radius: 0 8px 8px 0;
  margin-top: 1rem;
}

/* Table of contents */
nav.lesson-toc {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 12px;
  padding: 0.9rem 1.1rem;
  margin: 1.5rem 0;
}
nav.lesson-toc h2 { border: none; font-size: 1rem; margin: 0 0 0.4rem; color: var(--muted); text-transform: uppercase; letter-spacing: 0.06em; }
nav.lesson-toc ol { margin: 0; padding-left: 1.2rem; columns: 2; column-gap: 2rem; }
nav.lesson-toc li { margin: 0.25rem 0; break-inside: avoid; }
nav.lesson-toc a { text-decoration: none; }
nav.lesson-toc a:hover { text-decoration: underline; }
@media (max-width: 620px) { nav.lesson-toc ol { columns: 1; } }

/* Cards & panels */
.panel {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 12px;
  padding: 1.1rem 1.25rem;
}
.callout { border-left: 4px solid var(--info); }
.callout.tip { border-left-color: var(--success); }
.callout.warn { border-left-color: var(--warning); }

/* Objectives */
.objectives-list { padding-left: 1.3rem; }
.objectives-list li { margin: 0.45rem 0; }
.objectives-list li::marker { color: var(--teal); font-weight: 700; }

/* Terminology & tables */
.table-scroll { overflow-x: auto; -webkit-overflow-scrolling: touch; }
table { border-collapse: collapse; width: 100%; min-width: 540px; background: var(--surface); }
caption { text-align: left; color: var(--muted); font-size: 0.88rem; padding: 0.3rem 0; }
th, td { border: 1px solid var(--line); padding: 0.55rem 0.7rem; text-align: left; vertical-align: top; }
th { background: var(--surface-soft); color: var(--ink); }
tbody tr:nth-child(even) { background: var(--surface-soft); }
html[data-theme="dark"] tbody tr:nth-child(even) { background: var(--surface-soft); }

/* ============ Formula display (CSS stacked fractions) ============ */
.formula-box {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 12px;
  padding: 1rem 1.1rem;
  overflow-x: auto;
}
.math {
  font-family: Georgia, "Times New Roman", serif;
  font-size: 1.25rem;
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 0.45rem;
  color: var(--ink);
}
.frac { display: inline-flex; flex-direction: column; align-items: center; vertical-align: middle; margin: 0 0.15em; }
.frac .top { padding: 0 0.35em 0.1em; border-bottom: 1.5px solid var(--ink); }
.frac .bottom { padding: 0.1em 0.35em 0; }
.sup { font-size: 0.72em; vertical-align: super; }
.sym-list { list-style: none; padding-left: 0; margin: 0.8rem 0 0; }
.sym-list li { padding: 0.25rem 0; border-bottom: 1px dashed var(--line); }
.sym-list li:last-child { border-bottom: none; }
.sym { font-family: Georgia, serif; font-weight: 700; color: var(--teal-dark); padding-right: 0.4rem; }

/* ============ SVG figures ============ */
figure { margin: 1.4rem 0; }
figure svg { width: 100%; height: auto; display: block; }
figcaption { color: var(--muted); font-size: 0.9rem; margin-top: 0.4rem; }
.fig-panel { background: var(--surface); border: 1px solid var(--line); border-radius: 12px; padding: 0.8rem; }

/* ============ Interactive controls ============ */
.tool {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 14px;
  padding: 1.35rem 1.45rem;
}
.tool-title { display: flex; align-items: center; gap: 0.5rem; margin-top: 0; }
.controls { display: grid; grid-template-columns: repeat(auto-fit, minmax(210px, 1fr)); gap: 0.9rem 1.4rem; margin: 0.8rem 0; }
.control label { display: block; font-weight: 600; font-size: 0.95rem; margin-bottom: 0.25rem; }
.control input[type="range"] { width: 100%; accent-color: var(--teal); min-height: 32px; }
.control input[type="number"] {
  width: 100%;
  padding: 0.55rem 0.6rem;
  border: 1px solid var(--line);
  border-radius: 8px;
  background: var(--surface);
  color: var(--ink);
  font-size: 1rem;
}
.value-readout { font-variant-numeric: tabular-nums; color: var(--teal-dark); font-weight: 700; }
.btn {
  appearance: none;
  border: 1px solid var(--teal-dark);
  background: var(--teal);
  color: #fff;
  border-radius: 8px;
  padding: 0.55rem 1.1rem;
  font-size: 0.98rem;
  font-weight: 600;
  cursor: pointer;
  min-height: 44px;
}
html[data-theme="dark"] .btn { color: #0d1722; }
.btn.secondary { background: var(--surface-soft); color: var(--ink); border-color: var(--line); }
html[data-theme="dark"] .btn.secondary { color: var(--ink); }
.btn:hover { filter: brightness(1.05); }
.btn-row { display: flex; flex-wrap: wrap; gap: 0.6rem; margin-top: 0.8rem; }
.result-box {
  margin-top: 1rem;
  background: var(--surface-soft);
  border: 1px solid var(--line);
  border-radius: 12px;
  padding: 0.9rem 1rem;
}
.result-big { font-size: 1.35rem; font-weight: 700; color: var(--teal-dark); font-variant-numeric: tabular-nums; }
.error-msg { color: var(--danger); font-weight: 600; margin-top: 0.6rem; }
.method-note { font-size: 0.88rem; color: var(--muted); margin-top: 0.6rem; }

/* Reveal panels */
.reveal-panel { margin-top: 0.9rem; }
.reveal-body {
  border: 1px dashed var(--line);
  border-radius: 12px;
  padding: 0.9rem 1rem;
  margin-top: 0.6rem;
  background: var(--surface-soft);
}
[hidden] { display: none !important; }

/* Mistake cards */
.mistake { border-left: 4px solid var(--danger); background: var(--surface); border-radius: 0 8px 8px 0; padding: 0.8rem 1rem; margin: 0.9rem 0; }
.mistake h4 { margin: 0 0 0.3rem; }
.tag { display: inline-block; font-size: 0.75rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.05em; border-radius: 4px; padding: 0.1rem 0.45rem; margin-left: 0.4rem; background: var(--surface-soft); border: 1px solid var(--line); color: var(--ink-soft); }

/* ============ Quiz ============ */
.quiz-q { background: var(--surface); border: 1px solid 